# SharkFin
# SharkFin Data repository for the synthetic dataset used in the DASM article. This dataset servers the purpose of simulating a transactional data with a heavy density of transactional sizes concentrated around a particular size offset from 1, while also having a minimum transactional size in the whole dataset equal to one. 

This repository contains a .csv file of the dataset and the code used to generate it.
